(# Example 3D use)
(when flag
  (forever
    (pen_clear)
    (call drawTriangle3D
      (* (math sin (value timer) ) 100)
      -100
      (* (math cos (value timer) ) 100)
      (* (math sin (+ (value timer) 0.25) ) 100)
      -100
      (* (math cos (+ (value timer) 0.25) ) 100)
      0
      100
      1
    )
  )
)
